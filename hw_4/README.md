# 作业4：微分方程模型

> 作业 ddl：2025年5月17日（周日）06:00

> 迟交政策：5月20日课前提交不超过80分，课后提交不超过60分

> 提交内容：源代码 + 实验报告（pdf文件） + 其他补充说明文件（可选）

在复杂多变的现实世界中，许多自然现象和社会问题都蕴含着动态变化的规律，而这些规律往往可以通过微分方程（Differential Equations）来刻画。在本次实验中，我们将通过如下三个选项来练习求解微分方程的数值解、使用微分方程求解实际问题、利用微分方程得到高效算法。

## 选项 1：Deep Ritz

**关键词：机器学习，变分法，偏微分方程数值解**

偏微分方程数值解一直是科学计算领域最重要的课题之一。在过去的数十年中，数学家们对**有限差分法 (FDM, Finite Difference Method)**，**有限元法 (FEM, Finite Element Method)**，**谱方法 (Spectral Method)** 等传统方法进行了极其详尽的数值分析。然而，我们不难留意到这些方法都依赖于生成高质量的网格，这使得其求解复杂区域的 PDE 时往往面临困难。

近年来，随着机器学习领域的迅速发展和 **Universal Approximation Theorem**，不少学者开始关注其在 PDE 数值解领域的应用。基于将神经网络作为 PDE 解的近似器这一基本思想，**PINNs (Physics-Informed Neural Networks, 2019)**，**Deep Ritz (2018)**，**WAN (Weak Adversarial Networks, 2020)** 等无需网格的机器学习方法应运而生. 在本选项中, 我们的任务是复现 Deep Ritz 算法，理解其数学原理和思想。

考虑一具有变分结构的 Euler–Lagrange 方程，如 Poisson 方程。比如说，我们想要求解一 Dirichlet 边界条件的 Poisson 方程：

$$
\begin{cases}
-\Delta u(x) = f(x), & x \in \Omega, \\
u(x) = g(x), & x \in \partial\Omega.
\end{cases}
$$

变分法表明，求解此方程等价于最小化一能量泛函，见如下定理。

**定理** 考虑合适的函数空间，在容许函数类 $H=\\{ u \mid u(x)=g(x),\ x\in\partial\Omega \\}$ 上定义能量泛函

$$
J[u]=\int_{\Omega}\left(\frac12|\nabla u(x)|^2-f(x)u(x)\right)dx.
$$

若 $u^*=\arg\min_{u\in H}J[u]$，则

$$
\begin{cases}
-\Delta u^* (x) = f(x), & x \in \Omega, \\
u^* (x) = g(x), & x \in \partial\Omega.
\end{cases}
$$

请各位同学在报告中利用变分法证明此定理。

事实上，我们可以对能量泛函稍作修改，添加一边界罚项：

$$
J[u]=\int_{\Omega}\left(\frac12|\nabla u(x)|^2-f(x)u(x)\right) dx
+\beta\int_{\partial\Omega}(u(x)-g(x))^2 dS,
$$

其中 $\beta$ 为罚参数，一般取值较大。若能最小化此能量泛函，则对应的函数即为方程的解。

现在，我们可以用神经网络 $u(\theta)$ 来逼近函数的解 **(Universal Approximation Theorem)**，其中 $\theta$ 为网络参数。自然地，能量泛函可作为神经网络的损失函数：

$$
L(\theta)=\int_{\Omega}\left(\frac12|\nabla u(x;\theta)|^2-f(x)u(x;\theta)\right) dx
+\beta\int_{\partial\Omega}(u(x;\theta)-g(x))^2 dS.
$$

虽然我们无法直接计算积分，但可以在区域和边界上离散采样点以计算**数值积分 (Quadrature)**。此时，最小化损失函数即可得到方程的数值解。这就是 Deep Ritz 的基本原理。

此外，论文还给出了以下两个算法细节，鉴于其思想非常经典，故将其作为作业的必选内容：

- 使用**随机梯度下降 (SGD)**：每轮训练时，在区域和边界上随机采样小批量的点（独立采样，均匀分布）用于梯度下降。（加分项：若采用固定配点结果会如何？展示结果并分析 +2）

- 使用**残差连接 (ResNet)**：在神经网络中引入 ResNet 能够有效解决梯度消失问题并提高解的精度。（加分项：若不采用 ResNet 结构会如何？展示结果并分析 +2）

### 实验要求

- **阐述算法的基本原理** （包括数学原理、损失函数、网络结构、网络训练方法）
- **用变分法证明上文定理** （考虑到绝大部分同学没有学过 PDE2，证明不必过于严谨，如函数空间的选择可以忽略）
- **构造 Poisson 方程算例，使用 Deep Ritz 算法求解** （算例可以自己构造，也可以使用论文算例）
- **展示算例的数值结果** （若算例有解析解，可以计算数值解的 $L_2 / L_\infty$ 误差并制作表格；若算例没有解析解，可使用其他数值方法如 FDM、FEM 等得到参考解，将数值解和参考解可视化并对比）

注：
- 本选项适合对神经网络和微分方程有一定了解的同学
- 请提交环境配置文件 (C++ / Python)
- 不必制作图形用户界面 (GUI)

### 扣分点

- **用变分法给出定理的数学证明** (5)
- **说明损失函数的具体形式和数学原理** （证明最小化损失函数能够得到方程的解） (5)
- **说明网络结构和网络训练方法** （网络结构: ResNet, 网络训练方法: 配点的选取） (5)
- **给出算例的数值解, 计算其与解析解的误差 / 将其与其他方法得到的参考解做对比** (5)

### 加分点

- **上文所述的两个加分点** (每点 +2)
- **考虑解析解算例, 将 Deep Ritz 与其他方法 (如 FDM, FEM, PINNs, RFM 等) 作误差对比并分析各自的优缺点** (每种方法数学原理 +3, 数值结果和分析 +2)

参考文献：[Deep Ritz](https://link.springer.com/article/10.1007/s40304-018-0127-z)

## 选项 2：传染病周期性爆发的建模

许多传染病的病例数并不是单调上升或下降，而是呈现周期性爆发。例如，某些儿童传染病在没有大规模干预时会出现年度或两年度流行；某些疾病在长期时间尺度上还会出现周期改变、复发、甚至局部灭绝后再出现的现象。

本题要求从简单的SIR模型出发，通过数值实验逐步探索：哪些机制可能导致疫情的周期性爆发？足以解释长期持续振荡的最小模型是什么？

本题不要求使用真实疫情数据，但要求自己设计合理的参数，进行系统的数值实验，并对模型结果给出机制解释。

### 1. 基本 SIR 模型：单次疫情为何会结束？

设 $S(t)$ 表示时刻 $t$ 的易感者数量， $I(t)$ 表示时刻 $t$ 的感染者数量， $R(t)$ 表示时刻 $t$ 的移除者数量， $N$ 表示总人口数量， $\beta$ 表示传播率， $\gamma$ 表示恢复率。考虑基本 SIR 模型：

$$
\frac{dS}{dt}=-\beta SI
$$

$$
\frac{dI}{dt}=\beta SI-\gamma I
$$

$$
\frac{dR}{dt}=\gamma I
$$

要求：

1. 解释方程中每一项的现实含义。 (3)
2. 查找常见传染性疾病的参数估计，了解合理的参数范围（鼓励借助AI，但需要给出可靠的来源并自行验证）。 (2)
3. 选取不同的 $\beta$ 和 $\gamma$，用 Euler 法或 Runge-Kutta 法求解模型。 (2)
4. 画出 $S(t)$、 $I(t)$、 $R(t)$ 随时间变化的曲线，对其走势进行简要描述，并讨论参数的影响。 (2)
5. 观察 $I(t)$ 会最终趋于0还是会出现多次疫情峰值，并结合实际意义解释原因。 (3)
6. 通过数学分析或数值实验讨论：若初始易感者比例较低，疫情是否还能爆发？ (2)

### 2. 加入人口动态：复发疫情能否自然出现？

基本 SIR 模型中，疫情结束后没有新的易感者来源。现在考虑有出生和自然死亡的 SIR 模型。设 $\mu$ 表示自然死亡率，并假设出生率与死亡率平衡，使总人口规模近似保持不变。模型为：

$$
\frac{dS}{dt}=\mu N-\beta SI-\mu S
$$

$$
\frac{dI}{dt}=\beta SI-\gamma I-\mu I
$$

$$
\frac{dR}{dt}=\gamma I-\mu R
$$

要求：

1. 解释方程中每一项的现实含义（基本SIR模型中已经解释过的无需重复解释）。 (2)
2. 绘制状态变量岁时间变化的曲线，对其走势进行简要描述，并讨论参数的影响。 (2)
3. 与基本 SIR 模型相比，观察 $I(t)$ 是否可能出现多次峰值，并判断这些峰值振幅的变化情况。 (2)
4. 讨论：你认为出生死亡机制与周期性疫情的关系是什么？ (3)

### 3. 季节性传播率：学校开学会不会改变疫情周期？

现实中传播率可能不是常数。例如，儿童在上学期间接触更频繁，假期接触减少。令 $\beta(t)$ 表示随时间变化的传播率， $\beta_0$ 表示平均传播率， $\alpha$ 表示季节性强度， $t$ 表示以年为单位的时间。考虑：

$$
\beta(t)=\beta_0(1+\alpha \cos(2\pi t))
$$

其中 $0 \leq \alpha \leq 1$。

将第 2 部分模型中的常数 $\beta$ 替换为 $\beta(t)$，进行数值实验。

要求：

1. 固定 $\beta_0$、 $\gamma$、 $\mu$，改变 $\alpha$，进行数值模拟实验。 (2)
2. 对每组参数，画出长期 $I(t)$ 曲线。可以先模拟较长时间，再去掉前期暂态，只观察后期周期结构。 (2)
3. 描述后期疫情的动态：是趋于平衡、周期性振荡，还是更复杂的不规则振荡。 (3)
4. （选做）制作一张“参数扫描图”：横轴为 $\alpha$，纵轴记录长期每年同一时间的 $I(t)$ 值。根据图像判断不同 $\alpha$ 下周期结构是否发生变化，并进行简要讨论。 （画图 3，讨论 2）

### 4. （选做）加入随机性：小数量感染者为什么重要？

确定性 ODE 模型会把 $I(t)$ 当作连续变量，但现实中感染者人数是整数。当 $I(t)$ 很小时，随机事件可能决定疾病是否灭绝。推荐查阅并实现 Gillespie 算法，用事件驱动方式模拟随机 SIR 或 其他传染病模型。（也可以使用其他你认为合理的引入随机性的方法。）

要求：

1. 写出你所建立的随机模型的具体数学形式。 (2)
2. 解释你所用来求解模型的算法原理。 (2)
3. 对每组参数重复模拟至少 50 次，记录每次模拟中疾病是否在一定时间内灭绝，若灭绝，记录灭绝时间。 (2)
4. 比较确定性模型与随机模型的 $I(t)$ 曲线，说明二者的关系以及随机性对长期结果的影响。 (2)
5. 比较不同初始状态和人口规模 $N$ 下随机波动的影响。 (2)

注：没有标注（选做）的为扣分项，标注（选做）的为加分项，上限10分；本选项不要求GUI。


## 选项 3：Poisson 图像融合

实际上，这是计算机图形学课程的第三个作业，相关的算法原理可以参考文档：[计算机图形学课程文档](https://github.com/USTC-CG/USTC_CG_26/tree/main/Homeworks/3_poisson_image_editing)，本课程为该算法提供了一个Matlab框架，可参考[Matlab框架](./poissonediting)

### 实验要求

选项 3 的基本要求如下：
- 完成文中的 Seamless cloning 的 Importing gradients 部分，务必写清楚该算法的原理，首先需要从偏微分方程开始分析，再到稀疏矩阵方程的构造；
- 实时拖动区域显示结果，需要录制视频；
- 请采用多组测试样例（大于等于4组图片）来直观说明算法的有效性和鲁棒性。

注：
- 不选项不限制编程语言，可以利用框架[Matlab框架](./poissonediting)，也可以利用IMGUI等工具搭建C++框架，但是务必录制实时融合的视频；
- 由于Matlab框架可以实现不规则区域的融合，因此如果使用C++框架但没有实现该结果，会扣除很多分数。

#### 扣分点

- 实现 Seamless cloning 的 Import Gradients 部分算法（建模过程 + 算法实现）（5）
- 需要有 GUI（5）
- 上述 Seamless cloning 算法要做到实时运行（思考：如何做到实时？），并录制交互过程视频（5）
- 至少使用 **4 组图片** 进行测试（5）

#### 加分点

- 对梯度进行一些修正，比如使用Mixing gradients方法，并与现在的结果进行比较（5）
- Poisson 图像融合的其他应用（一个 5 分）
